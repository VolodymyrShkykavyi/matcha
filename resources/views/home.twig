{% extends 'layouts/app.twig' %}

{% block content %}

    <div class="row">
        <div class="col-12">
            <div class="top-header mb-2">
                <div class="top-header-thumb">
                    <img src="img/top-header1.jpg" alt="nature">
                </div>
                <div class="profile-section bg-white p-3">
                    <div class="row">

                    </div>
                </div>
            </div>
        </div>
    </div>
    <form action="{{ path_for('user.login', {id: 1}) }}" method="post">
        <input type="text" name="email">
        <button type="submit">OK</button>
    </form>
    args:<br>
    <pre>
        {#{{ users }}#}
    </pre>

    <h3>dump:</h3><br>
    <pre>
{{ dump() }}
    </pre>

{% endblock %}